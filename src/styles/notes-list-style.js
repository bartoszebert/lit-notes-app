import { css } from "lit";

export const notesListStyles = css`
  #notes-list {
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  #notes-list li {
    list-style: none;
  }
`;
